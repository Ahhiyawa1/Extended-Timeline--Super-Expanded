spriteTypes = {
	spriteType = { # Background patch to cover eu4's date year
		name = date_gui_bg
		texturefile = "interface/date_gui_bg.dds"
	}
	spriteType = { # Smaller spacer, default space width is 4px which is a bit too wide
		name = GFX_2px
		texturefile = "interface/2px.dds"
	}
}