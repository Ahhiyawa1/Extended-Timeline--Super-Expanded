## 
## Disabled due to bugs, lag, and other unpleasantries.
## 


# # Claim Colonial (Sub-)Region
# country_decisions = {

	# claim_algeria = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# algeria_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = ALG } }
			# NOT = { has_global_flag = claimed_algeria_global }
		# }
		# provinces_to_highlight = {
			# algeria_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# algeria_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_algeria_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = ALG } }
					# }
					# every_province = {
						# limit = { algeria_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# algeria_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_algeria
			# set_global_flag = claimed_algeria_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_algeria = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_algeria
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# algeria_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = ALG } }
			# NOT = { has_global_flag = organized_algeria }
		# }
		# provinces_to_highlight = {
			# algeria_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 22 # 80% of total
				# algeria_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_algeria_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = ALG } }
					# }
					# ALG = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# algeria_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = ALG
					# }
				# }
			# }
			# set_global_flag = organized_algeria
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# algeria_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_morocco = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# morocco_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = MOR } }
			# NOT = { has_global_flag = claimed_morocco_global }
		# }
		# provinces_to_highlight = {
			# morocco_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# morocco_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_morocco_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = MOR } }
					# }
					# every_province = {
						# limit = { morocco_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# morocco_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_morocco
			# set_global_flag = claimed_morocco_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_morocco = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_morocco
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# morocco_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = MOR } }
			# NOT = { has_global_flag = organized_morocco }
		# }
		# provinces_to_highlight = {
			# morocco_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 18 # 80% of total
				# morocco_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_morocco_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = MOR } }
					# }
					# MOR = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# morocco_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = MOR
					# }
				# }
			# }
			# set_global_flag = organized_morocco
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# morocco_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_tunisia = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# tunisia_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = TUN } }
			# NOT = { has_global_flag = claimed_tunisia_global }
		# }
		# provinces_to_highlight = {
			# tunisia_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 3
				# tunisia_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_tunisia_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = TUN } }
					# }
					# every_province = {
						# limit = { tunisia_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# tunisia_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_tunisia
			# set_global_flag = claimed_tunisia_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_tunisia = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_tunisia
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# tunisia_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = TUN } }
			# NOT = { has_global_flag = organized_tunisia }
		# }
		# provinces_to_highlight = {
			# tunisia_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 8 # 80% of total
				# tunisia_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_tunisia_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = TUN } }
					# }
					# TUN = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# tunisia_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = TUN
					# }
				# }
			# }
			# set_global_flag = organized_tunisia
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# tunisia_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_egypt = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# egypt_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = EGY } }
			# NOT = { has_global_flag = claimed_egypt_global }
		# }
		# provinces_to_highlight = {
			# egypt_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# egypt_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_egypt_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = EGY } }
					# }
					# every_province = {
						# limit = { egypt_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# egypt_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_egypt
			# set_global_flag = claimed_egypt_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_egypt = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_egypt
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# egypt_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = EGY } }
			# NOT = { has_global_flag = organized_egypt }
		# }
		# provinces_to_highlight = {
			# egypt_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 22 # 80% of total
				# egypt_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_egypt_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = EGY } }
					# }
					# EGY = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# egypt_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = EGY
					# }
				# }
			# }
			# set_global_flag = organized_egypt
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# egypt_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_benin = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# benin_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = BEN } }
			# NOT = { has_global_flag = claimed_benin_global }
		# }
		# provinces_to_highlight = {
			# benin_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2
				# benin_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_benin_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = BEN } }
					# }
					# every_province = {
						# limit = { benin_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# benin_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_benin
			# set_global_flag = claimed_benin_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_benin = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_benin
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# benin_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = BEN } }
			# NOT = { has_global_flag = organized_benin }
		# }
		# provinces_to_highlight = {
			# benin_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 4 # 80% of total
				# benin_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_benin_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = BEN } }
					# }
					# BEN = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# benin_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = BEN
					# }
				# }
			# }
			# set_global_flag = organized_benin
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# benin_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_ethiopia = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# ethiopia_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = ETH } }
			# NOT = { has_global_flag = claimed_ethiopia_global }
		# }
		# provinces_to_highlight = {
			# ethiopia_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# ethiopia_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_ethiopia_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = ETH } }
					# }
					# every_province = {
						# limit = { ethiopia_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# ethiopia_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_ethiopia
			# set_global_flag = claimed_ethiopia_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_ethiopia = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_ethiopia
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# ethiopia_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = ETH } }
			# NOT = { has_global_flag = organized_ethiopia }
		# }
		# provinces_to_highlight = {
			# ethiopia_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 26 # 80% of total
				# ethiopia_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_ethiopia_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = ETH } }
					# }
					# ETH = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# ethiopia_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = ETH
					# }
				# }
			# }
			# set_global_flag = organized_ethiopia
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# ethiopia_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_mali = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# mali_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = MAL } }
			# NOT = { has_global_flag = claimed_mali_global }
		# }
		# provinces_to_highlight = {
			# mali_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# mali_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_mali_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = MAL } }
					# }
					# every_province = {
						# limit = { mali_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# mali_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_mali
			# set_global_flag = claimed_mali_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_mali = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_mali
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# mali_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = MAL } }
			# NOT = { has_global_flag = organized_mali }
		# }
		# provinces_to_highlight = {
			# mali_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 17 # 80% of total
				# mali_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_mali_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = MAL } }
					# }
					# MAL = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# mali_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = MAL
					# }
				# }
			# }
			# set_global_flag = organized_mali
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# mali_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_somalia = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# somalia_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = SOM } }
			# NOT = { has_global_flag = claimed_somalia_global }
		# }
		# provinces_to_highlight = {
			# somalia_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# somalia_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_somalia_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = SOM } }
					# }
					# every_province = {
						# limit = { somalia_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# somalia_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_somalia
			# set_global_flag = claimed_somalia_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_somalia = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_somalia
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# somalia_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = SOM } }
			# NOT = { has_global_flag = organized_somalia }
		# }
		# provinces_to_highlight = {
			# somalia_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 14 # 80% of total
				# somalia_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_somalia_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = SOM } }
					# }
					# SOM = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# somalia_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = SOM
					# }
				# }
			# }
			# set_global_flag = organized_somalia
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# somalia_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_zimbabwe = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# zimbabwe_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = GZI } }
			# NOT = { has_global_flag = claimed_zimbabwe_global }
		# }
		# provinces_to_highlight = {
			# zimbabwe_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# zimbabwe_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_zimbabwe_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = GZI } }
					# }
					# every_province = {
						# limit = { zimbabwe_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# zimbabwe_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_zimbabwe
			# set_global_flag = claimed_zimbabwe_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_zimbabwe = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_zimbabwe
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# zimbabwe_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = GZI } }
			# NOT = { has_global_flag = organized_zimbabwe }
		# }
		# provinces_to_highlight = {
			# zimbabwe_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 7 # 80% of total
				# zimbabwe_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_zimbabwe_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = GZI } }
					# }
					# GZI = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# zimbabwe_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = GZI
					# }
				# }
			# }
			# set_global_flag = organized_zimbabwe
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# zimbabwe_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_libya = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# libya_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = LBY } }
			# NOT = { has_global_flag = claimed_libya_global }
		# }
		# provinces_to_highlight = {
			# libya_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# libya_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_libya_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = LBY } }
					# }
					# every_province = {
						# limit = { libya_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# libya_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_libya
			# set_global_flag = claimed_libya_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# #organize_libya = {
	# #	major = yes
	# #	potential = {
	# #		has_country_flag = claimed_libya
	# #		num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
	# #			value = 1
	# #			libya_trigger = yes
	# #		}
	# #		NOT = { started_in = 3998.1.1 }
	# #		NOT = { alias_exists = { tag = LBY } }
	# #		NOT = { has_global_flag = organized_libya }
	# #	}
	# #	provinces_to_highlight = {
	# #		libya_trigger = yes
	# #		NOT = { country_or_non_sovereign_subject_holds = ROOT }
	# #	}
	# #	allow = {
	# #		is_at_war = no
	# #		num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
	# #			value = 12 # 80% of total
	# #			libya_trigger = yes
	# #		}
	# #	}
	# #	effect = {
	# #		custom_tooltip = organize_libya_tt
	# #		hidden_effect = {
	# #			if = {
	# #				limit = { # Limit effect for use in hist.
	# #					NOT = { alias_exists = { tag = LBY } }
	# #				}
	# #				LBY = {
	# #					change_government = republic
	# #					every_core_province = {
	# #						remove_core = PREV
	# #					}
	# #				}
	# #				every_province = {
	# #					limit = {
	# #						libya_trigger = yes
	# #						country_or_non_sovereign_subject_holds = ROOT
	# #					}
	# #					every_country_including_inactive = {
	# #						limit = {
	# #							capital_scope = {
	# #								always = yes
	# #							}
	# #							PREV = {
	# #								is_core = PREV
	# #							}
	# #							exists = no
	# #						}
	# #						PREV = {
	# #							remove_core = PREV
	# #						}
	# #					}
	# #					add_core = LBY
	# #				}
	# #			}
	# #		}
	# #		set_global_flag = organized_libya
	# #	}
	# #	ai_will_do = {
	# #		factor = 1
	# #		modifier = {
	# #			factor = 0
	# #			any_province = { # Natives left to conquer
	# #				libya_trigger = yes
	# #				owner = {
	# #					is_colonial_nation = no
	# #					capital_scope = {
	# #						continent = africa
	# #					}
	# #				}
	# #			}
	# #		}
	# #	}
	# #	ai_importance = 200
	# #}

	# claim_angola = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# angola_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = AGL } }
			# NOT = { has_global_flag = claimed_angola_global }
		# }
		# provinces_to_highlight = {
			# angola_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# angola_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_angola_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = AGL } }
					# }
					# every_province = {
						# limit = { angola_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# angola_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_angola
			# set_global_flag = claimed_angola_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_angola = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_angola
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# angola_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = AGL } }
			# NOT = { has_global_flag = organized_angola }
		# }
		# provinces_to_highlight = {
			# angola_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 18 # 80% of total
				# angola_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_angola_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = AGL } }
					# }
					# AGL = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# angola_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = AGL
					# }
				# }
			# }
			# set_global_flag = organized_angola
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# angola_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_burkina_faso = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# burkina_faso_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = BFA } }
			# NOT = { has_global_flag = claimed_burkina_faso_global }
		# }
		# provinces_to_highlight = {
			# burkina_faso_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 3
				# burkina_faso_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_burkina_faso_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = BFA } }
					# }
					# every_province = {
						# limit = { burkina_faso_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# burkina_faso_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_burkina_faso
			# set_global_flag = claimed_burkina_faso_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_burkina_faso = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_burkina_faso
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# burkina_faso_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = BFA } }
			# NOT = { has_global_flag = organized_burkina_faso }
		# }
		# provinces_to_highlight = {
			# burkina_faso_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 6 # 80% of total
				# burkina_faso_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_burkina_faso_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = BFA } }
					# }
					# BFA = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# burkina_faso_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = BFA
					# }
				# }
			# }
			# set_global_flag = organized_burkina_faso
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# burkina_faso_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_botswana = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# botswana_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = BOT } }
			# NOT = { has_global_flag = claimed_botswana_global }
		# }
		# provinces_to_highlight = {
			# botswana_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2
				# botswana_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_botswana_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = BOT } }
					# }
					# every_province = {
						# limit = { botswana_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# botswana_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_botswana
			# set_global_flag = claimed_botswana_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_botswana = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_botswana
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# botswana_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = BOT } }
			# NOT = { has_global_flag = organized_botswana }
		# }
		# provinces_to_highlight = {
			# botswana_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5 # 80% of total
				# botswana_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_botswana_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = BOT } }
					# }
					# BOT = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# botswana_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = BOT
					# }
				# }
			# }
			# set_global_flag = organized_botswana
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# botswana_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_central_africa = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# central_africa_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = CAF } }
			# NOT = { has_global_flag = claimed_central_africa_global }
		# }
		# provinces_to_highlight = {
			# central_africa_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2
				# central_africa_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_central_africa_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = CAF } }
					# }
					# every_province = {
						# limit = { central_africa_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# central_africa_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_central_africa
			# set_global_flag = claimed_central_africa_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_central_africa = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_central_africa
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# central_africa_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = CAF } }
			# NOT = { has_global_flag = organized_central_africa }
		# }
		# provinces_to_highlight = {
			# central_africa_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5 # 80% of total
				# central_africa_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_central_africa_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = CAF } }
					# }
					# CAF = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# central_africa_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = CAF
					# }
				# }
			# }
			# set_global_flag = organized_central_africa
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# central_africa_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_chad = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# chad_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = CDD } }
			# NOT = { has_global_flag = claimed_chad_global }
		# }
		# provinces_to_highlight = {
			# chad_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 4
				# chad_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_chad_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = CDD } }
					# }
					# every_province = {
						# limit = { chad_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# chad_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_chad
			# set_global_flag = claimed_chad_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_chad = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_chad
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# chad_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = CDD } }
			# NOT = { has_global_flag = organized_chad }
		# }
		# provinces_to_highlight = {
			# chad_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 9 # 80% of total
				# chad_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_chad_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = CDD } }
					# }
					# CDD = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# chad_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = CDD
					# }
				# }
			# }
			# set_global_flag = organized_chad
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# chad_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_cameroon = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# cameroon_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = CMR } }
			# NOT = { has_global_flag = claimed_cameroon_global }
		# }
		# provinces_to_highlight = {
			# cameroon_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2
				# cameroon_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_cameroon_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = CMR } }
					# }
					# every_province = {
						# limit = { cameroon_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# cameroon_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_cameroon
			# set_global_flag = claimed_cameroon_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_cameroon = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_cameroon
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# cameroon_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = CMR } }
			# NOT = { has_global_flag = organized_cameroon }
		# }
		# provinces_to_highlight = {
			# cameroon_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 6 # 80% of total
				# cameroon_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_cameroon_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = CMR } }
					# }
					# CMR = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# cameroon_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = CMR
					# }
				# }
			# }
			# set_global_flag = organized_cameroon
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# cameroon_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_congo_brazzaville = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# congo_brazzaville_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = CNG } }
			# NOT = { has_global_flag = claimed_congo_brazzaville_global }
		# }
		# provinces_to_highlight = {
			# congo_brazzaville_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# congo_brazzaville_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_congo_brazzaville_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = CNG } }
					# }
					# every_province = {
						# limit = { congo_brazzaville_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# congo_brazzaville_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_congo_brazzaville
			# set_global_flag = claimed_congo_brazzaville_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_congo_brazzaville = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_congo_brazzaville
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# congo_brazzaville_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = CNG } }
			# NOT = { has_global_flag = organized_congo_brazzaville }
		# }
		# provinces_to_highlight = {
			# congo_brazzaville_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 3 # 80% of total
				# congo_brazzaville_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_congo_brazzaville_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = CNG } }
					# }
					# CNG = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# congo_brazzaville_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = CNG
					# }
				# }
			# }
			# set_global_flag = organized_congo_brazzaville
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# congo_brazzaville_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_djibouti = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# djibouti_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = DJI } }
			# NOT = { has_global_flag = claimed_djibouti_global }
		# }
		# provinces_to_highlight = {
			# djibouti_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# djibouti_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_djibouti_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = DJI } }
					# }
					# every_province = {
						# limit = { djibouti_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# djibouti_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_djibouti
			# set_global_flag = claimed_djibouti_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_djibouti = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_djibouti
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# djibouti_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = DJI } }
			# NOT = { has_global_flag = organized_djibouti }
		# }
		# provinces_to_highlight = {
			# djibouti_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1 # 80% of total
				# djibouti_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_djibouti_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = DJI } }
					# }
					# DJI = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# djibouti_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = DJI
					# }
				# }
			# }
			# set_global_flag = organized_djibouti
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# djibouti_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_congo_kinshasa = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# congo_kinshasa_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = DRC } }
			# NOT = { has_global_flag = claimed_congo_kinshasa_global }
		# }
		# provinces_to_highlight = {
			# congo_kinshasa_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# congo_kinshasa_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_congo_kinshasa_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = DRC } }
					# }
					# every_province = {
						# limit = { congo_kinshasa_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# congo_kinshasa_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_congo_kinshasa
			# set_global_flag = claimed_congo_kinshasa_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_congo_kinshasa = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_congo_kinshasa
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# congo_kinshasa_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = DRC } }
			# NOT = { has_global_flag = organized_congo_kinshasa }
		# }
		# provinces_to_highlight = {
			# congo_kinshasa_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 22 # 80% of total
				# congo_kinshasa_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_congo_kinshasa_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = DRC } }
					# }
					# DRC = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# congo_kinshasa_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = DRC
					# }
				# }
			# }
			# set_global_flag = organized_congo_kinshasa
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# congo_kinshasa_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_equatorial_guinea = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# equatorial_guinea_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = EQG } }
			# NOT = { has_global_flag = claimed_equatorial_guinea_global }
		# }
		# provinces_to_highlight = {
			# equatorial_guinea_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# equatorial_guinea_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_equatorial_guinea_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = EQG } }
					# }
					# every_province = {
						# limit = { equatorial_guinea_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# equatorial_guinea_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_equatorial_guinea
			# set_global_flag = claimed_equatorial_guinea_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_equatorial_guinea = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_equatorial_guinea
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# equatorial_guinea_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = EQG } }
			# NOT = { has_global_flag = organized_equatorial_guinea }
		# }
		# provinces_to_highlight = {
			# equatorial_guinea_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2 # 80% of total
				# equatorial_guinea_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_equatorial_guinea_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = EQG } }
					# }
					# EQG = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# equatorial_guinea_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = EQG
					# }
				# }
			# }
			# set_global_flag = organized_equatorial_guinea
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# equatorial_guinea_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_eritrea = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# eritrea_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = ERT } }
			# NOT = { has_global_flag = claimed_eritrea_global }
		# }
		# provinces_to_highlight = {
			# eritrea_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2
				# eritrea_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_eritrea_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = ERT } }
					# }
					# every_province = {
						# limit = { eritrea_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# eritrea_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_eritrea
			# set_global_flag = claimed_eritrea_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_eritrea = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_eritrea
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# eritrea_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = ERT } }
			# NOT = { has_global_flag = organized_eritrea }
		# }
		# provinces_to_highlight = {
			# eritrea_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 4 # 80% of total
				# eritrea_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_eritrea_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = ERT } }
					# }
					# ERT = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# eritrea_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = ERT
					# }
				# }
			# }
			# set_global_flag = organized_eritrea
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# eritrea_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_gabon = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# gabon_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = GAB } }
			# NOT = { has_global_flag = claimed_gabon_global }
		# }
		# provinces_to_highlight = {
			# gabon_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# gabon_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_gabon_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = GAB } }
					# }
					# every_province = {
						# limit = { gabon_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# gabon_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_gabon
			# set_global_flag = claimed_gabon_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_gabon = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_gabon
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# gabon_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = GAB } }
			# NOT = { has_global_flag = organized_gabon }
		# }
		# provinces_to_highlight = {
			# gabon_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 3 # 80% of total
				# gabon_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_gabon_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = GAB } }
					# }
					# GAB = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# gabon_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = GAB
					# }
				# }
			# }
			# set_global_flag = organized_gabon
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# gabon_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_guinea_bissau = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# guinea_bissau_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = GBI } }
			# NOT = { has_global_flag = claimed_guinea_bissau_global }
		# }
		# provinces_to_highlight = {
			# guinea_bissau_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# guinea_bissau_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_guinea_bissau_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = GBI } }
					# }
					# every_province = {
						# limit = { guinea_bissau_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# guinea_bissau_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_guinea_bissau
			# set_global_flag = claimed_guinea_bissau_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_guinea_bissau = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_guinea_bissau
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# guinea_bissau_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = GBI } }
			# NOT = { has_global_flag = organized_guinea_bissau }
		# }
		# provinces_to_highlight = {
			# guinea_bissau_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1 # 80% of total
				# guinea_bissau_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_guinea_bissau_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = GBI } }
					# }
					# GBI = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# guinea_bissau_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = GBI
					# }
				# }
			# }
			# set_global_flag = organized_guinea_bissau
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# guinea_bissau_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_ghana = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# ghana_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = GHA } }
			# NOT = { has_global_flag = claimed_ghana_global }
		# }
		# provinces_to_highlight = {
			# ghana_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2
				# ghana_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_ghana_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = GHA } }
					# }
					# every_province = {
						# limit = { ghana_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# ghana_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_ghana
			# set_global_flag = claimed_ghana_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_ghana = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_ghana
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# ghana_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = GHA } }
			# NOT = { has_global_flag = organized_ghana }
		# }
		# provinces_to_highlight = {
			# ghana_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 6 # 80% of total
				# ghana_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_ghana_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = GHA } }
					# }
					# GHA = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# ghana_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = GHA
					# }
				# }
			# }
			# set_global_flag = organized_ghana
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# ghana_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_gambia = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# gambia_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = GMB } }
			# NOT = { has_global_flag = claimed_gambia_global }
		# }
		# provinces_to_highlight = {
			# gambia_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# gambia_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_gambia_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = GMB } }
					# }
					# every_province = {
						# limit = { gambia_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# gambia_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_gambia
			# set_global_flag = claimed_gambia_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_gambia = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_gambia
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# gambia_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = GMB } }
			# NOT = { has_global_flag = organized_gambia }
		# }
		# provinces_to_highlight = {
			# gambia_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1 # 80% of total
				# gambia_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_gambia_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = GMB } }
					# }
					# GMB = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# gambia_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = GMB
					# }
				# }
			# }
			# set_global_flag = organized_gambia
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# gambia_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_guinea = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# guinea_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = GUI } }
			# NOT = { has_global_flag = claimed_guinea_global }
		# }
		# provinces_to_highlight = {
			# guinea_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2
				# guinea_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_guinea_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = GUI } }
					# }
					# every_province = {
						# limit = { guinea_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# guinea_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_guinea
			# set_global_flag = claimed_guinea_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_guinea = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_guinea
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# guinea_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = GUI } }
			# NOT = { has_global_flag = organized_guinea }
		# }
		# provinces_to_highlight = {
			# guinea_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 4 # 80% of total
				# guinea_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_guinea_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = GUI } }
					# }
					# GUI = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# guinea_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = GUI
					# }
				# }
			# }
			# set_global_flag = organized_guinea
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# guinea_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_ivory_coast = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# ivory_coast_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = IVO } }
			# NOT = { has_global_flag = claimed_ivory_coast_global }
		# }
		# provinces_to_highlight = {
			# ivory_coast_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2
				# ivory_coast_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_ivory_coast_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = IVO } }
					# }
					# every_province = {
						# limit = { ivory_coast_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# ivory_coast_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_ivory_coast
			# set_global_flag = claimed_ivory_coast_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_ivory_coast = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_ivory_coast
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# ivory_coast_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = IVO } }
			# NOT = { has_global_flag = organized_ivory_coast }
		# }
		# provinces_to_highlight = {
			# ivory_coast_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 6 # 80% of total
				# ivory_coast_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_ivory_coast_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = IVO } }
					# }
					# IVO = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# ivory_coast_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = IVO
					# }
				# }
			# }
			# set_global_flag = organized_ivory_coast
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# ivory_coast_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_kenya = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# kenya_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = KEY } }
			# NOT = { has_global_flag = claimed_kenya_global }
		# }
		# provinces_to_highlight = {
			# kenya_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 4
				# kenya_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_kenya_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = KEY } }
					# }
					# every_province = {
						# limit = { kenya_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# kenya_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_kenya
			# set_global_flag = claimed_kenya_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_kenya = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_kenya
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# kenya_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = KEY } }
			# NOT = { has_global_flag = organized_kenya }
		# }
		# provinces_to_highlight = {
			# kenya_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 9 # 80% of total
				# kenya_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_kenya_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = KEY } }
					# }
					# KEY = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# kenya_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = KEY
					# }
				# }
			# }
			# set_global_flag = organized_kenya
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# kenya_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_liberia = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# liberia_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = LBR } }
			# NOT = { has_global_flag = claimed_liberia_global }
		# }
		# provinces_to_highlight = {
			# liberia_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# liberia_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_liberia_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = LBR } }
					# }
					# every_province = {
						# limit = { liberia_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# liberia_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_liberia
			# set_global_flag = claimed_liberia_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_liberia = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_liberia
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# liberia_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = LBR } }
			# NOT = { has_global_flag = organized_liberia }
		# }
		# provinces_to_highlight = {
			# liberia_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2 # 80% of total
				# liberia_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_liberia_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = LBR } }
					# }
					# LBR = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# liberia_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = LBR
					# }
				# }
			# }
			# set_global_flag = organized_liberia
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# liberia_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_madagascar = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# madagascar_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = MDG } }
			# NOT = { has_global_flag = claimed_madagascar_global }
		# }
		# provinces_to_highlight = {
			# madagascar_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# madagascar_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_madagascar_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = MDG } }
					# }
					# every_province = {
						# limit = { madagascar_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# madagascar_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_madagascar
			# set_global_flag = claimed_madagascar_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_madagascar = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_madagascar
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# madagascar_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = MDG } }
			# NOT = { has_global_flag = organized_madagascar }
		# }
		# provinces_to_highlight = {
			# madagascar_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 12 # 80% of total
				# madagascar_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_madagascar_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = MDG } }
					# }
					# MDG = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# madagascar_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = MDG
					# }
				# }
			# }
			# set_global_flag = organized_madagascar
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# madagascar_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_mauritania = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# mauritania_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = MRT } }
			# NOT = { has_global_flag = claimed_mauritania_global }
		# }
		# provinces_to_highlight = {
			# mauritania_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 3
				# mauritania_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_mauritania_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = MRT } }
					# }
					# every_province = {
						# limit = { mauritania_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# mauritania_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_mauritania
			# set_global_flag = claimed_mauritania_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_mauritania = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_mauritania
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# mauritania_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = MRT } }
			# NOT = { has_global_flag = organized_mauritania }
		# }
		# provinces_to_highlight = {
			# mauritania_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 7 # 80% of total
				# mauritania_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_mauritania_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = MRT } }
					# }
					# MRT = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# mauritania_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = MRT
					# }
				# }
			# }
			# set_global_flag = organized_mauritania
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# mauritania_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_malawi = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# malawi_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = MWI } }
			# NOT = { has_global_flag = claimed_malawi_global }
		# }
		# provinces_to_highlight = {
			# malawi_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# malawi_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_malawi_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = MWI } }
					# }
					# every_province = {
						# limit = { malawi_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# malawi_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_malawi
			# set_global_flag = claimed_malawi_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_malawi = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_malawi
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# malawi_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = MWI } }
			# NOT = { has_global_flag = organized_malawi }
		# }
		# provinces_to_highlight = {
			# malawi_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 3 # 80% of total
				# malawi_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_malawi_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = MWI } }
					# }
					# MWI = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# malawi_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = MWI
					# }
				# }
			# }
			# set_global_flag = organized_malawi
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# malawi_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_mozambique = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# mozambique_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = MZM } }
			# NOT = { has_global_flag = claimed_mozambique_global }
		# }
		# provinces_to_highlight = {
			# mozambique_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# mozambique_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_mozambique_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = MZM } }
					# }
					# every_province = {
						# limit = { mozambique_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# mozambique_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_mozambique
			# set_global_flag = claimed_mozambique_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_mozambique = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_mozambique
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# mozambique_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = MZM } }
			# NOT = { has_global_flag = organized_mozambique }
		# }
		# provinces_to_highlight = {
			# mozambique_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 15 # 80% of total
				# mozambique_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_mozambique_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = MZM } }
					# }
					# MZM = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# mozambique_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = MZM
					# }
				# }
			# }
			# set_global_flag = organized_mozambique
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# mozambique_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_niger = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# niger_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = NGR } }
			# NOT = { has_global_flag = claimed_niger_global }
		# }
		# provinces_to_highlight = {
			# niger_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# niger_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_niger_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = NGR } }
					# }
					# every_province = {
						# limit = { niger_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# niger_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_niger
			# set_global_flag = claimed_niger_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_niger = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_niger
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# niger_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = NGR } }
			# NOT = { has_global_flag = organized_niger }
		# }
		# provinces_to_highlight = {
			# niger_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 12 # 80% of total
				# niger_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_niger_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = NGR } }
					# }
					# NGR = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# niger_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = NGR
					# }
				# }
			# }
			# set_global_flag = organized_niger
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# niger_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_nigeria = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# nigeria_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = NIG } }
			# NOT = { has_global_flag = claimed_nigeria_global }
		# }
		# provinces_to_highlight = {
			# nigeria_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# nigeria_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_nigeria_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = NIG } }
					# }
					# every_province = {
						# limit = { nigeria_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# nigeria_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_nigeria
			# set_global_flag = claimed_nigeria_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_nigeria = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_nigeria
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# nigeria_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = NIG } }
			# NOT = { has_global_flag = organized_nigeria }
		# }
		# provinces_to_highlight = {
			# nigeria_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 22 # 80% of total
				# nigeria_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_nigeria_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = NIG } }
					# }
					# NIG = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# nigeria_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = NIG
					# }
				# }
			# }
			# set_global_flag = organized_nigeria
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# nigeria_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_namibia = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# namibia_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = NMB } }
			# NOT = { has_global_flag = claimed_namibia_global }
		# }
		# provinces_to_highlight = {
			# namibia_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# namibia_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_namibia_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = NMB } }
					# }
					# every_province = {
						# limit = { namibia_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# namibia_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_namibia
			# set_global_flag = claimed_namibia_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_namibia = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_namibia
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# namibia_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = NMB } }
			# NOT = { has_global_flag = organized_namibia }
		# }
		# provinces_to_highlight = {
			# namibia_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 6 # 80% of total
				# namibia_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_namibia_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = NMB } }
					# }
					# NMB = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# namibia_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = NMB
					# }
				# }
			# }
			# set_global_flag = organized_namibia
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# namibia_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_senegal = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# senegal_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = SGL } }
			# NOT = { has_global_flag = claimed_senegal_global }
		# }
		# provinces_to_highlight = {
			# senegal_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2
				# senegal_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_senegal_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = SGL } }
					# }
					# every_province = {
						# limit = { senegal_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# senegal_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_senegal
			# set_global_flag = claimed_senegal_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_senegal = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_senegal
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# senegal_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = SGL } }
			# NOT = { has_global_flag = organized_senegal }
		# }
		# provinces_to_highlight = {
			# senegal_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 4 # 80% of total
				# senegal_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_senegal_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = SGL } }
					# }
					# SGL = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# senegal_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = SGL
					# }
				# }
			# }
			# set_global_flag = organized_senegal
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# senegal_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_somaliland = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# somaliland_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = SML } }
			# NOT = { has_global_flag = claimed_somaliland_global }
		# }
		# provinces_to_highlight = {
			# somaliland_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2
				# somaliland_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_somaliland_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = SML } }
					# }
					# every_province = {
						# limit = { somaliland_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# somaliland_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_somaliland
			# set_global_flag = claimed_somaliland_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_somaliland = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_somaliland
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# somaliland_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = SML } }
			# NOT = { has_global_flag = organized_somaliland }
		# }
		# provinces_to_highlight = {
			# somaliland_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5 # 80% of total
				# somaliland_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_somaliland_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = SML } }
					# }
					# SML = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# somaliland_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = SML
					# }
				# }
			# }
			# set_global_flag = organized_somaliland
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# somaliland_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_sierra_leone = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# sierra_leone_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = SRA } }
			# NOT = { has_global_flag = claimed_sierra_leone_global }
		# }
		# provinces_to_highlight = {
			# sierra_leone_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# sierra_leone_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_sierra_leone_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = SRA } }
					# }
					# every_province = {
						# limit = { sierra_leone_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# sierra_leone_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_sierra_leone
			# set_global_flag = claimed_sierra_leone_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_sierra_leone = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_sierra_leone
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# sierra_leone_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = SRA } }
			# NOT = { has_global_flag = organized_sierra_leone }
		# }
		# provinces_to_highlight = {
			# sierra_leone_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2 # 80% of total
				# sierra_leone_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_sierra_leone_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = SRA } }
					# }
					# SRA = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# sierra_leone_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = SRA
					# }
				# }
			# }
			# set_global_flag = organized_sierra_leone
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# sierra_leone_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_sudan = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# sudan_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = SUD } }
			# NOT = { has_global_flag = claimed_sudan_global }
		# }
		# provinces_to_highlight = {
			# sudan_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# sudan_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_sudan_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = SUD } }
					# }
					# every_province = {
						# limit = { sudan_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# sudan_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_sudan
			# set_global_flag = claimed_sudan_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_sudan = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_sudan
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# sudan_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = SUD } }
			# NOT = { has_global_flag = organized_sudan }
		# }
		# provinces_to_highlight = {
			# sudan_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 22 # 80% of total
				# sudan_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_sudan_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = SUD } }
					# }
					# SUD = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# sudan_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = SUD
					# }
				# }
			# }
			# set_global_flag = organized_sudan
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# sudan_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_togo = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# togo_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = TGO } }
			# NOT = { has_global_flag = claimed_togo_global }
		# }
		# provinces_to_highlight = {
			# togo_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# togo_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_togo_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = TGO } }
					# }
					# every_province = {
						# limit = { togo_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# togo_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_togo
			# set_global_flag = claimed_togo_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_togo = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_togo
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# togo_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = TGO } }
			# NOT = { has_global_flag = organized_togo }
		# }
		# provinces_to_highlight = {
			# togo_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2 # 80% of total
				# togo_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_togo_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = TGO } }
					# }
					# TGO = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# togo_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = TGO
					# }
				# }
			# }
			# set_global_flag = organized_togo
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# togo_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_tanzania = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# tanzania_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = TNZ } }
			# NOT = { has_global_flag = claimed_tanzania_global }
		# }
		# provinces_to_highlight = {
			# tanzania_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# tanzania_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_tanzania_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = TNZ } }
					# }
					# every_province = {
						# limit = { tanzania_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# tanzania_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_tanzania
			# set_global_flag = claimed_tanzania_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_tanzania = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_tanzania
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# tanzania_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = TNZ } }
			# NOT = { has_global_flag = organized_tanzania }
		# }
		# provinces_to_highlight = {
			# tanzania_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 17 # 80% of total
				# tanzania_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_tanzania_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = TNZ } }
					# }
					# TNZ = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# tanzania_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = TNZ
					# }
				# }
			# }
			# set_global_flag = organized_tanzania
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# tanzania_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_uganda = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# uganda_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = UGA } }
			# NOT = { has_global_flag = claimed_uganda_global }
		# }
		# provinces_to_highlight = {
			# uganda_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2
				# uganda_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_uganda_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = UGA } }
					# }
					# every_province = {
						# limit = { uganda_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# uganda_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_uganda
			# set_global_flag = claimed_uganda_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_uganda = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_uganda
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# uganda_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = UGA } }
			# NOT = { has_global_flag = organized_uganda }
		# }
		# provinces_to_highlight = {
			# uganda_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5 # 80% of total
				# uganda_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_uganda_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = UGA } }
					# }
					# UGA = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# uganda_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = UGA
					# }
				# }
			# }
			# set_global_flag = organized_uganda
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# uganda_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_western_sahara = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# western_sahara_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = WSH } }
			# NOT = { has_global_flag = claimed_western_sahara_global }
		# }
		# provinces_to_highlight = {
			# western_sahara_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# western_sahara_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_western_sahara_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = WSH } }
					# }
					# every_province = {
						# limit = { western_sahara_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# western_sahara_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_western_sahara
			# set_global_flag = claimed_western_sahara_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_western_sahara = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_western_sahara
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# western_sahara_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = WSH } }
			# NOT = { has_global_flag = organized_western_sahara }
		# }
		# provinces_to_highlight = {
			# western_sahara_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 2 # 80% of total
				# western_sahara_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_western_sahara_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = WSH } }
					# }
					# WSH = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# western_sahara_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = WSH
					# }
				# }
			# }
			# set_global_flag = organized_western_sahara
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# western_sahara_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_zambia = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# zambia_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = ZAM } }
			# NOT = { has_global_flag = claimed_zambia_global }
		# }
		# provinces_to_highlight = {
			# zambia_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 3
				# zambia_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_zambia_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = ZAM } }
					# }
					# every_province = {
						# limit = { zambia_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# zambia_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_zambia
			# set_global_flag = claimed_zambia_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_zambia = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_zambia
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# zambia_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = ZAM } }
			# NOT = { has_global_flag = organized_zambia }
		# }
		# provinces_to_highlight = {
			# zambia_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 8 # 80% of total
				# zambia_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_zambia_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = ZAM } }
					# }
					# ZAM = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# zambia_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = ZAM
					# }
				# }
			# }
			# set_global_flag = organized_zambia
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# zambia_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

	# claim_south_africa = {
		# major = yes
		# potential = {
			# has_global_flag = berlin_conference_triggered
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# south_africa_trigger = yes
			# }
			# is_civilized = yes
			# capital_scope = {
				# NOT = { continent = africa }
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = SAF } }
			# NOT = { has_global_flag = claimed_south_africa_global }
		# }
		# provinces_to_highlight = {
			# south_africa_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 5
				# south_africa_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = claim_south_africa_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = SAF } }
					# }
					# every_province = {
						# limit = { south_africa_trigger = yes }
						# # Give Permanent Claims
						# if = {
							# limit = {
								# NOT = { country_or_non_sovereign_subject_holds = ROOT }
								# NOT = { is_core = ROOT }
							# }
							# add_permanent_claim = ROOT
						# }
						# # Steal subject's provinces
						# if = {
							# limit = {
								# owner = {
									# is_subject_of = ROOT
									# is_subject_other_than_tributary_trigger = yes
									# OR = {
										# is_colonial_nation = no
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
							# }
							# if = {
								# limit = {
									# owner = {
										# capital_scope = {
											# NOT = { continent = africa }
										# }
									# }
								# }
								# remove_core = owner
							# }
							# add_territorial_core = ROOT
							# cede_province = ROOT
						# }
					# }
					# # Sphere local natives
					# if = {
						# limit = { is_great_power = yes }
						# every_country = {
							# limit = {
								# is_subject = no
								# capital_scope = {
									# south_africa_trigger = yes
								# }
							# }
							# PREV = {
								# add_to_sphere = { tag = PREV }
								# add_diplomatic_influence = {
									# who = PREV
									# value = 99
								# }
							# }
						# }
					# }
				# }
			# }
			# set_country_flag = claimed_south_africa
			# set_global_flag = claimed_south_africa_global
		# }
		# ai_will_do = {
			# factor = 1
		# }
		# ai_importance = 200
	# }

	# organize_south_africa = {
		# major = yes
		# potential = {
			# has_country_flag = claimed_south_africa
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 1
				# south_africa_trigger = yes
			# }
			# NOT = { started_in = 3998.1.1 }
			# NOT = { alias_exists = { tag = RSA } }
			# NOT = { has_global_flag = organized_south_africa }
		# }
		# provinces_to_highlight = {
			# south_africa_trigger = yes
			# NOT = { country_or_non_sovereign_subject_holds = ROOT }
		# }
		# allow = {
			# is_at_war = no
			# num_of_provinces_owned_or_owned_by_non_sovereign_subjects_with = {
				# value = 14 # 80% of total
				# south_africa_trigger = yes
			# }
		# }
		# effect = {
			# custom_tooltip = organize_south_africa_tt
			# hidden_effect = {
				# if = {
					# limit = { # Limit effect for use in hist.
						# NOT = { alias_exists = { tag = RSA } }
					# }
					# SAF = {
						# change_government = republic
						# every_core_province = {
							# remove_core = PREV
						# }
					# }
					# every_province = {
						# limit = {
							# south_africa_trigger = yes
							# country_or_non_sovereign_subject_holds = ROOT
						# }
						# every_country_including_inactive = {
							# limit = {
								# capital_scope = {
									# always = yes
								# }
								# PREV = {
									# is_core = PREV
								# }
								# exists = no
							# }
							# PREV = {
								# remove_core = PREV
							# }
						# }
						# add_core = SAF
					# }
				# }
			# }
			# set_global_flag = organized_south_africa
		# }
		# ai_will_do = {
			# factor = 1
			# modifier = {
				# factor = 0
				# any_province = { # Natives left to conquer
					# south_africa_trigger = yes
					# owner = {
						# is_colonial_nation = no
						# capital_scope = {
							# continent = africa
						# }
					# }
				# }
			# }
		# }
		# ai_importance = 200
	# }

# }